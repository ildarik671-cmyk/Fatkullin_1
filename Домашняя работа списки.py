# Задача 1 (Создание списка и получение элементов)
# Создайте список tools, содержащий в себе три строки: "отвертка", "пассатижи", "молоток".
# Затем выведите на экран (с помощью print) второй элемент этого списка.

tools = ["отвертка", "пассатижи", "молоток"]
print(tools [1] )


# Задача 2 (Добавление элементов в список)
# У вас есть начальный список: numbers = [10, 20].
# С помощью подходящего метода добавьте в его конец число 30.
# Затем выведите изменённый список на экран.

numbers = [10, 20]
numbers.append ( 30 )
print(numbers)


# Задача 3 (Замена элементов в списке)
# У вас есть список: colors = ["красный", "синий", "желтый"].
# Замените второй элемент этого списка на строку "зеленый".
# Затем выведите изменённый список на экран.

colors = ["красный", "синий", "желтый"]
colors[1] = "зеленый"
print(colors)


# **Задача 4 (Удаление элементов из списка)**
# У вас есть список: `fruits = ["яблоко", "банан", "киви", "банан", "груша"]`.
# Удалите из этого списка **первое** вхождение строки `"банан"` с помощью подходящего метода.
# Затем выведите изменённый список на экран.

fruits = ["яблоко", "банан", "киви", "банан", "груша"]
fruits.remove("банан")
print(fruits)


# Задача 5 (Срезы списка)
# У вас есть список: letters = ["a", "b", "c", "d", "e", "f"].
# Создайте новый список с именем slice_result, который будет содержать элементы letters со второго по четвертый включительно (по индексам). Используйте срез.
# Выведите список slice_result на экран.

letters = ["a", "b", "c", "d", "e", "f"]
slice_result = letters[1:4]
print(slice_result)


# **Задача 6 (Слияние списков)**
# У вас есть два списка:
# `list_a = [1, 2, 3]`
# `list_b = [4, 5, 6]`
# Создайте новый список с именем `merged`, который является результатом **слияния** этих двух списков (сперва элементы `list_a`, затем `list_b`). Используйте для этого оператор `+`.
# Выведите список `merged` на экран.

list_a = [1, 2, 3]
list_b = [4, 5, 6]
merged = list_a + list_b
print(merged)


# **Задача 7 (Добавление элементов в конкретное место списка)**
# У вас есть список: `animals = ["собака", "хомяк", "попугай"]`.
# Вставьте строку `"кошка"` в этот список **между** `"собака"` и `"хомяк"`. Используйте для этого подходящий метод, который принимает индекс и элемент.
# Выведите изменённый список на экран.

animals = ["собака", "хомяк", "попугай"]
animals.insert(1, "кошка")
print(animals)


# **Задача 8 (Удаление элементов из списка и его очистка)**
# У вас есть список: `data = [True, 42, "текст", 3.14, False]`.
# 1.  Удалите элемент из этого списка **по индексу 2** с помощью подходящего метода или оператора.
# 2.  После этого **полностью очистите** оставшийся список, сделав его пустым, с помощью специального метода.
# Выведите на экран список после очистки.

data = [True, 42, "текст", 3.14, False]
data.pop(2)
data.clear()
print(data)


# **Задача 9 (Получение элементов списка и базовые операции)**
# У вас есть список: `values = [5, 15, 25, 35]`.
# 1.  Получите **последний** элемент этого списка, используя **отрицательный индекс**, и сохраните его в переменной `last_value`.
# 2.  Получите **первый** элемент списка, сохраните его в переменной `first_value`.
# 3.  Выведите на экран **сумму** значений `first_value` и `last_value`.

values = [5, 15, 25, 35]
last_value = values[-1]
first_value = values[0]
result = last_value + first_value
print(result)


# **Задача 10 (Создание списка и добавление элементов)**
# 1.  Создайте **пустой** список с именем `shopping_list`.
# 2.  Добавьте в этот список (в конец) три строки: `"хлеб"`, `"молоко"`, `"яйца"`. Сделайте это **одной командой**,
# используя метод, который может добавлять несколько элементов за раз.
# 3.  Выведите итоговый список на экран.

shopping_list = []
shopping_list2= ["хлеб", "молоко", "яйца"]
shopping_list.extend(shopping_list2)
print(shopping_list)



# Смешанная задача 1 (Операции со списком)
# У вас есть список: items = ["книга", "ручка", "карандаш", "ластик", "книга"].
# Выполните следующие действия по порядку и выводите список на экран после каждого шага:
# Добавьте в конец списка строку "тетрадь".
# Вставьте строку "линейка" на позицию между "ручка" и "карандаш".
# Удалите первое вхождение строки "книга" из списка.
# Получите срез списка, содержащий элементы со второго по пятый (включительно, по порядку в списке),
# и сохраните его в переменной selected_items. Выведите на экран только этот срез.
# Очистите весь исходный список items.
# В результате у вас должно быть 5 команд print (после каждого шага с 1 по 4 и после шага 5).

items = ["книга", "ручка", "карандаш", "ластик", "книга"]
items.append("тетрадь")
print(items)
items.insert(2,"линейка")
print(items)
items.remove("книга")
print(items)
selected_items = items[1:5]
print(selected_items)
items.clear()
print(items)


# **Задача №1**
# Создай список `fruits`,
# который содержит три любых фрукта (строки). Затем выведи на экран (с помощью `print`)
# второй элемент этого списка (напоминаю, что индексация начинается с нуля).

fruits = ["банан","яблоко","джетфрут"]
frut = fruits[1]
print(frut)


# **Задача №2**
# У тебя есть список чисел: `numbers = [10, 20, 30, 40, 50]`.
# Используя срез (slice), получи из него новый список, который содержит элементы со **второго по четвертый включительно**
# (индексы 1, 2, 3) и выведи этот новый список на экран.

numbers = [10, 20, 30, 40, 50]
slice_result = numbers[1:4]
print(slice_result)


# **Задача №3**
# У тебя есть список: `colors = ['красный', 'зеленый', 'синий']`.
# Добавь в конец этого списка цвет `'желтый'`, используя соответствующий метод. Затем выведи итоговый список на экран.

colors = ['красный', 'зеленый', 'синий']
colors.append('желтый')
print(colors)


# **Задача №4**
# У тебя есть список: `items = ['карандаш', 'ручка', 'линейка']`.
# Необходимо заменить второй элемент списка (сейчас это `'ручка'`) на `'ластик'`. После замены выведи список `items` на экран.

items = ['карандаш', 'ручка', 'линейка']
items[1] = 'ластик'
print(items)


# **Задача №5**
# У тебя есть два списка:
# `list_a = [1, 2, 3]`
# `list_b = [4, 5, 6]`
# Создай новый список `list_c`, который будет результатом слияния (объединения) списков `list_a` и `list_b`
# (сначала элементы `list_a`, потом `list_b`). Выведи `list_c` на экран.

list_a = [1, 2, 3]
list_b = [4, 5, 6]
list_c = list_a + list_b
print(list_c)


# **Задача №6**
# У тебя есть список: `animals = ['кошка', 'собака', 'попугай', 'рыбка']`.
# Удали из этого списка элемент `'попугай'`, используя метод `remove()`. Выведи обновлённый список на экран.

animals = ['кошка', 'собака', 'попугай', 'рыбка']
animals.remove("попугай")
print(animals)


# **Задача №7**
# У тебя есть список: `chars = ['a', 'b', 'c', 'd']`.
# Вставь символ `'x'` на позицию с индексом 2 (т.е. так, чтобы он стал третьим элементом списка).
# Используй метод `insert()`. Выведи итоговый список.

chars = ['a', 'b', 'c', 'd']
chars.insert(2,"x")
print(chars)


# **Задача №8**
# У тебя есть список: `nums = [100, 200, 300, 400, 500]`.
# Используя срез, выведи на экран все элементы этого списка, начиная с индекса 1 и до конца списка.

nums = [100, 200, 300, 400, 500]
slice_result = nums[1::]
print(slice_result)


# **Задача №9**
# У тебя есть список: `data = [10, 20, 30, 40, 50]`.
# Очисти этот список полностью (сделай его пустым), используя соответствующий метод. Затем выведи очищенный список на экран.

data = [10, 20, 30, 40, 50]
data.clear()
print(data)


# **Задача №10**
# У тебя есть список: `letters = ['a', 'b', 'c', 'd', 'e']`.
# Выведи на экран последний элемент этого списка, используя отрицательный индекс.

letters = ['a', 'b', 'c', 'd', 'e']
print(letters[-1])
