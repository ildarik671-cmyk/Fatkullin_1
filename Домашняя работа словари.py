# Задача 1
# Раздел: Проверка на наличие ключей и значений
#
# Создайте словарь inventory с товарами: 'apples' — 5, 'bananas' — 2, 'oranges' — 9.
# Напишите код, который проверяет, есть ли в словаре товар 'bananas'. Результат проверки (True или False) выведите на экран.

inventory = {
    "apples": 5,
    "bananas": 2,
    "oranges": 9
}
print("bananas" in inventory)



# Задача 2
# Раздел: Удаление элементов словаря
# Создайте словарь student с ключами: "name" (значение — ваше имя), "age" (ваш возраст), "city" (ваш город).
# Удалите из этого словаря элемент с ключом "city" с помощью соответствующего метода для словарей.
# Выведите получившийся словарь на экран.

student = {
    "name": "Ильдар",
    "age": 34,
    "city": "Стерлитамак"
}
student.pop("city")
print(student)


# Задача 3
# Раздел: Получение элементов, ключей и значений словаря
# Создайте словарь colors с названиями цветов на русском и их кодами в формате HEX: "красный" — "#FF0000", "зеленый" — "#00FF00", "синий" — "#0000FF".
# Выведите на экран три отдельные строки:
# Все ключи словаря.
# Все значения словаря.
# Все пары "ключ-значение" словаря.
# Каждый вывод должен быть в виде списка.

colors = {
    "красный" : "#FF0000",
    "зеленый" : "#00FF00",
    "синий" : "#0000FF"
}
res1 = colors.keys()
res2 = colors.values()
res3 = colors.items()
print(list(res1))
print(list(res2))
print(list(res3))


# Задача 4
# Раздел: Доступ к элементам словаря по ключу
# Создайте словарь book с ключами: "title" (значение — "1984"), "author" (значение — "Джордж Оруэлл"), "year" (значение — 1949).
# Напишите код, который получает и выводит на экран значение для ключа "author", используя квадратные скобки [].

book = {
    "title": "1984",
    "author": "Джордж Оруэлл",
    "year": 1949
}
print(book ["author"])


# Задача 5
# Раздел: Создание словаря
# Создайте пустой словарь с именем my_dict. Затем добавьте в него три элемента с помощью оператора присваивания:
# Ключ "fruit" со значением "apple"
# Ключ "count" со значением 3
# Ключ "price" со значением 100.5
# Выведите получившийся словарь на экран.

my_dict = {}
my_dict["fruit"] = "apple"
my_dict["count"] = 3
my_dict["price"] = 100.5
print(my_dict)


# **Задача 6**
# *Раздел: Добавление и обновление элементов словаря*
# Создайте словарь `car` с одним элементом: ключ `"brand"` со значением `"Toyota"`.
# Затем выполните две операции:
# 1. **Добавьте** новый элемент с ключом `"model"` и значением `"Camry"`.
# 2. **Обновите** значение элемента с ключом `"brand"` на `"Lexus"`.
# Выведите итоговый словарь на экран.

car = {
    "brand": "Toyota"
}
car["model"] = "Camry"
car["brand"] = "Lexus"
print(car)

# Задача 7
# # Раздел: Проверка на наличие ключей и значений
# # Создайте словарь phone_book с элементами: "Alice" — "123-456", "Bob" — "789-012", "Charlie" — "345-678".
# # Напишите код, который проверяет, есть ли в словаре значение "789-012". Результат проверки (True или False) выведите на экран.
# # Используйте метод для проверки значений, а не ключей.

phone_book = {
    "Alice" : "123-456",
    "Bob" : "789-012",
    "Charlie" : "345-678"
}
print("789-012" in phone_book.values())



# Задача 8 (обновленная)
# Раздел: Доступ к элементам словаря по ключу
# Создайте словарь country_capitals с элементами: "Россия" — "Москва", "Франция" — "Париж", "Германия" — "Берлин".
# Напишите код, который получает и выводит на экран столицу Франции, используя только доступ по ключу через квадратные скобки []

country_capitals = {
    "Россия" : "Москва",
    "Франция" : "Париж",
    "Германия" : "Берлин"
}
slolica = country_capitals["Франция"]
print(slolica)



# Задача 9
# Раздел: Добавление и обновление элементов словаря
# У вас есть словарь config с настройками: "language" — "Russian", "theme" — "dark".
# Пользователь решил сменить тему на светлую. Обновите значение ключа "theme" на "light".
# Пользователь добавил новую настройку — уровень звука. Добавьте ключ "volume" со значением 80.
# Выведите итоговый словарь на экран.

config = {
    "language" : "Russian",
    "theme" : "dark"
}
config["theme"] = "light"
config["volume"] = 80
print(config)


# Задача 10
# Раздел: Получение элементов, ключей и значений словаря
# Создайте словарь movie с элементами: "title" — "Интерстеллар", "year" — 2014, "director" — "Нолан".
# Напишите код, который выводит на экран только значения этого словаря в виде обычного списка (не объекта dict_values).

movie = {
    "title" : "Интерстеллар",
    "year" : 2014,
    "director" : "Нолан"
}
print(list(movie.values()))


# **Задача 1 (Создание словаря)**
# Создайте словарь `person` с тремя парами ключ-значение:
# - Ключ `"name"`, значение `"Анна"`
# - Ключ `"age"`, значение `25`
# - Ключ `"city"`, значение `"Москва"`
# Выведите весь словарь на экран.

person = {"name":"Анна",
          "age":25,
          "city":"Москва"}
print(person)

# **Задача 2 (Доступ к элементам словаря по ключу)**
# У вас есть словарь:
# `book = {"title": "Война и мир", "author": "Толстой", "year": 1869}`
# Получите из словаря значение по ключу `"author"` и сохраните его в переменную `author_name`.
# Затем выведите эту переменную на экран.

book = {"title": "Война и мир", "author": "Толстой", "year": 1869}
author_name = book["author"]
print(author_name)


# **Задача 3 (Добавление и обновление элементов словаря)**
# У вас есть словарь:
# `car = {"brand": "Toyota", "model": "Camry"}`
# 1.  Добавьте в словарь новую пару: ключ `"year"` со значением `2020`.
# 2.  Обновите значение по ключу `"model"` на `"Corolla"`.
# Выведите итоговый словарь на экран.

car = {"brand": "Toyota", "model": "Camry"}
car["year"] = 2020
car["model"] = "Corolla"
print(car)

# **Задача 4 (Удаление элементов словаря)**
# У вас есть словарь:
# `student = {"name": "Иван", "age": 20, "group": "А-101", "city": "Москва"}`
# 1.  Удалите из словаря элемент с ключом `"city"` с помощью подходящего метода или оператора.
# 2.  Удалите элемент с ключом `"group"` (любым способом).
# Выведите итоговый словарь на экран.

student = {"name": "Иван", "age": 20, "group": "А-101", "city": "Москва"}
student.pop("city")
del student ["group"]
print(student)

# **Задача 5 (Получение элементов, ключей и значений словаря)**
# У вас есть словарь:
# `laptop = {"brand": "HP", "ram": 16, "ssd": 512}`
# 1.  Получите **только ключи** словаря с помощью соответствующего метода и сохраните их в переменную `keys`.
# 2.  Получите **только значения** словаря и сохраните их в переменную `values`.
# 3.  Выведите на экран сначала `keys`, затем `values`.

laptop = {"brand": "HP", "ram": 16, "ssd": 512}
keys = laptop.keys()
values = laptop.values()
print(keys)
print(values)


# **Задача 6 (Проверка на наличие ключей и значений)**
# У вас есть словарь:
# `goods = {"apple": 50, "banana": 30, "orange": 40, "grape": 70}`
# 1.  Проверьте, есть ли в словаре **ключ** `"banana"`. Результат проверки (True или False) сохраните в переменную `has_banana` и выведите её.
# 2.  Проверьте, есть ли в словаре **значение** `100`. Результат сохраните в переменную `has_price_100` и выведите её.
# Используйте для проверок подходящие операторы или методы.

goods = {"apple": 50, "banana": 30, "orange": 40, "grape": 70}
has_banana = "banana" in goods
print(has_banana)
has_price_100 = 100 in goods.values()
print(has_price_100)


# **Задача 7 (Создание словаря и доступ по ключу)**
# Создайте словарь `phone`, который содержит следующие характеристики телефона:
# - Ключ `"brand"`, значение `"Samsung"`
# - Ключ `"model"`, значение `"Galaxy S21"`
# - Ключ `"year"`, значение `2021`
# Затем:
# 1.  Получите значение по ключу `"model"` и сохраните в переменную `model_name`.
# 2.  Получите значение по ключу `"year"` и сохраните в переменную `release_year`.
# 3.  Выведите на экран сообщение вида: `"Модель: Galaxy S21, год: 2021"` (используя ваши переменные).

phone = {"brand": "Samsung", "model": "Galaxy S21", "year": 2021}
model_name = phone["model"]
release_year = phone["year"]
print(f"Модель: {model_name}, год: {release_year}")


# **Задача 8 (Добавление и обновление элементов словаря)**
# У вас есть словарь:
# `profile = {"username": "python_dev", "status": "active"}`
# 1.  Добавьте в словарь новую пару: ключ `"age"` со значением `22`.
# 2.  Добавьте ещё одну новую пару: ключ `"email"` со значением `"dev@example.com"`.
# 3.  Обновите значение по ключу `"status"` на `"busy"`.
# Выведите итоговый словарь на экран.

profile = {"username": "python_dev", "status": "active"}
profile["age"] = 22
profile["email"] = "dev@example.com"
profile.update(status = "busy")
print(profile)

# **Задача 9 (Получение элементов словаря)**
# У вас есть словарь:
# `film = {"title": "Матрица", "director": "Вачовски", "year": 1999, "rating": 8.7}`
# 1.  Получите значение по ключу `"director"` **безопасным методом** (который не вызовет ошибку, если ключа вдруг нет) и сохраните в переменную `director_name`.
# 2.  Попробуйте получить значение по ключу `"budget"` тем же безопасным методом, со значением по умолчанию `"неизвестно"`. Сохраните результат в переменную `budget_info`.
# 3.  Выведите обе переменные на экран (каждую с новой строки).

film = {"title": "Матрица", "director": "Вачовски", "year": 1999, "rating": 8.7}
director_name = film.get("director")
budget_info= film.get("budget", "неизвестно")
print(director_name)
print(budget_info)


# **Задача 10 (Проверка на наличие ключей и значений)**
# У вас есть словарь:
# `inventory = {"apples": 10, "bananas": 5, "oranges": 8, "grapes": 0}`
# 1.  Проверьте, есть ли в словаре **ключ** `"pears"`. Результат сохраните в переменную `has_pears` и выведите её.
# 2.  Проверьте, есть ли в словаре **значение** `0`. Результат сохраните в переменную `has_zero` и выведите её.
# 3.  Проверьте, есть ли в словаре **пара** с ключом `"bananas"` и значением `5` (одновременно). Результат сохраните в переменную `has_bananas_five` и выведите её.
# Для последней проверки нужно использовать комбинацию: сначала проверить наличие ключа, затем сравнить его значение.

inventory = {"apples": 10, "bananas": 5, "oranges": 8, "grapes": 0}
has_pears = "pears" in inventory
print(has_pears)
has_zero = 0 in inventory.values()
print(has_zero)
has_bananas_five = "bananas" in inventory and inventory["bananas"] == 5
print(has_bananas_five)

# **Задача 1.**
# Создай словарь `student`, который будет содержать информацию о студенте: имя (`name`) — "Анна",
# возраст (`age`) — 20, и город (`city`) — "Москва". Просто создай этот словарь и сохрани его в переменную.

student = { "name" : "Анна", "age" : 20, "city" : "Москва"}



# **Задача 2 (повтор).**
# У тебя есть словарь `student = {"name": "Анна", "age": 20, "city": "Москва"}`.
# Напиши код, который выведет на экран возраст студента (значение по ключу "age").

student = {"name": "Анна", "age": 20, "city": "Москва"}
stud_age = student.get("age")
print(stud_age)


# **Задача 3.**
# У нас есть словарь с продуктом:
# `product = {"name": "Хлеб", "price": 50, "quantity": 10}`.
# В этом словаре измени цену (`price`) на 60 и добавь новый ключ `"discount"` со значением 10. Выведи итоговый словарь `product` на экран.

product = {"name": "Хлеб", "price": 50, "quantity": 10}
product["price"] = 60
product["discount"] = 10
print(product)


# **Задача 4.**
# Дан словарь:
# `user = {"id": 101, "username": "coder123", "email": "coder@mail.com"}`.
# Удали из этого словаря ключ `"email"` и выведи оставшийся словарь на экран.

user = {"id": 101, "username": "coder123", "email": "coder@mail.com"}
user.pop("email")
print(user)


# **Задача 5.**
# Дан словарь с настройками:
# `settings = {"theme": "dark", "language": "RU", "notifications": True}`.
# Получи и выведи на экран отдельно:
# 1. Список всех ключей словаря `settings`.
# 2. Список всех значений словаря `settings`.

settings = {"theme": "dark", "language": "RU", "notifications": True}
print(settings.keys())
print(settings.values())


# **Задача 6.**
# Дан словарь:
# `inventory = {"apples": 5, "bananas": 0, "oranges": 3}`.
# Напиши код, который проверяет, есть ли в словаре ключ `"bananas"`, и если есть, то выведи сообщение "Ключ bananas существует".
# А также проверь, есть ли среди значений число `0`, и если есть, выведи сообщение "Есть нулевое значение". (Используй оператор `in`).

inventory = {"apples": 5, "bananas": 0, "oranges": 3}
if "bananas" in inventory:
    print("Ключ bananas существует")
if 0 in inventory.values():
    print("Есть нулевое значение")
